defmodule Espy.Kademlia.Protocol do
  alias Espy.Kademlia.Node

  @ping_pool Application.get_env(:espy, :pools)[:name]

  def ping() do

  end

  def store(node, value) do
  end

  def find_node(id) do
  end

  def find_value(id) do
  end
end
