defmodule Espy.Kademlia.BucketTest do
  use ExUnit.Case
  doctest Espy

  test "can add a contact to the bucket" do
    assert Espy.hello() == :world
  end

  test "can add max contacts to the bucket" do

  end

  test "can add more than max contacts to the bucket, adding extras to replacements list" do

  end

  test "contact can be found" do

  end

  test "nonexistent contact cannot be found" do
  end

  
end
